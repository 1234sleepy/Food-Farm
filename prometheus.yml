global:
  
scrape_configs:
  - job_name: shop-scrape
    scrape_interval: 10s
    scrape_timeout: 3s
    static_configs:
      - targets:
         - shop-api:5000
        labels:
          app: shop-docker
      - targets:
         - host.docker.internal:6001
        labels:
          app: shop